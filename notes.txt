6/25:
  - DONE: Show background image that I will try to trace
    - Hardcode it for now, eventually have a way to choose or drag-n-drop
    - Can I link to image on another server, or do I have to download locally?
      - Can use remote
  - Moving lines and control points around works nicely!
  - Need to add and remove points
    DONE: 1. Remove control points with right click
    2. Remove entire part with right click (on end point)
      - How do we handle removing a part in the middle of sequence?
        - Change it to M so next part has a starting spot?
    - Think on how to add new lines (M) versus continue lines (L)
  - UI to show result
    - Side panel (easier to see each part individually)
    - Show list of parts, have number controls to change values
      - Update number pickers when points dragged, update points when number pickers changed
    - Separate panel with string at bottom? Or button to copy string?
  - DONE: Save image position and path so we can restore on page refresh
    - Saving all GameState

  - Adding new parts
    - For now, default to "insert" mode
      - show line to potential new endpoint
    - Refactor: one "cursor" variable?
      - status hover/selected/new
      - location
      - whether moving
      - IN PROGRESS

  - Maybe hold off on refactor for the moment, need to think more about this
    - I can have a currently selected point and a place I'm hovering
    - I'm thinking of clicking on an endpoint to say I want to continue the sequence
      - And then draw a dotted line from the selected endpoint to the place my mouse is currently hovering
      - So these can't be replaced by just one "cursor"

  - Think about creating a series from scratch
    - Click to start sequence (adds an M <point>)
    - Click again for next point (adds an L <point>)

  - Controls
    - Left click to create point
      - Left clicking on existing line would add a point there
    - Middle click to move point?
      - Distinguish creating point on existing line to creating control point and moving it
    - Right click to remove point
      - Feels straightforward
    - Control snaps to line/curve (makes sure we don't snap to control points!)
    - Need way to snap to alignment with previous control points

  - Make viewport be 1x1? Or -1 to positive 1, centered at 0?
    - I think -1 being left-most and 1 being right-most is easiest to conceptualize
    - Radius of 1 is simplest

6/24:
  - Create control points by dragging
    - One kind of click for arc, another for curves?
    - Less interested in arcs at the moment, start with curves
      1. Show potential control point when hover over line
        - This could get yucky for bezier curves...
          - quadratic bezier seems at least possible (but quite a bit of code)
          - I'm afraid to ask about cubic bezier...
            - Sounds like it can't be solved directly
            - But I can calculate a bunch of points on the curve and find the closest one of those
              - That's probably fine
              - For bonus points, I can try "Newton-Raphson" refinement
        - Maybe check if we are within polygon formed by endpoints and control points?
          - Can we be within more than one of those at a time?
      2. Clicking with potential control point makes it an actual control point and sets it active
      - Start with 1 for quadratic, then allow 2 for cubic

  - DONE: NEXT: Check 10 points of curve for closest

  - DONE: Visual: draw faint dotted lines between endpoints and controlpoints 
    - so it's clear which belongs to which

6/23:
  - DONE: Re-roll if limb color already active

  - Start playing with SVG path editor
    - Begin with points
    - Add control points?
      - I could click on line and drag out to circle, that looked good with arcs
      - Not sure how it would look for bezier curves
    - Save path as list of tokens (so we can more easily insert, delete, etc)
      - Flatten to an actual string when saving it

  - DONE: Starting with a set of lines and curves that can have their points and control points moved

6/22:
  - DONE: Choose random limb/color (number 0-15)
    - Limbs: right foot, left hand, left food, right hand (clockwise from angle 0)
    - Colors: red, green, yellow, blue
  - DONE: Eventually keep track of which color for each limb
    - NEXT: ...and don't choose it again

6/18:

Idea: Create SVG path editor
  - To help me trace the jpeg's of twister boards
  - Create lines, then drag control points?
  - Try to get a curve from the average of a bunch of hand-drawn lines (delta's)?
  - Play with some different ideas here
  - Draw all the points and control points in a given path, let user drag them around
    - or edit string manually, and show result

6/17:

DONE: Spin randomly with click

Draw board (simple for now)
 - Labels, icons, colors DONE
 - Needs black lines to separate limbs

Eventually, make it spin enough to hit a given value (randomly determined in advance)
  - This lets us make sure it doesn't land on one we already have chosen
  - To verify it's working, it should land exactly in the middle of the chosen value
  - Later, to make it look at little better, give it some random offset (but still be near middle)

Keep track of chosen color for each limb
  - Mark with black circle? Or something

DONE: Spin to specified point
  - Figure out starting velocity by putting deltaAngle into equations of motion

6/16:

Twister spinner idea
- Start with a triangle that spins continuously
- Then try one that starts fast and slows to a stop
